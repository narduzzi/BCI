function data=electrodes_selection(signal,header,label)
%select electrodes of interest
%INPUT : signal, header, label
%OUTPUT : data is the signal for each elec

idx=index_of_electrodes(label,header);

data=signal(idx,:);

end